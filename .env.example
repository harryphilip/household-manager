# Django Settings
# WARNING: Generate a strong secret key for production!
# Run: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here

# SECURITY WARNING: Set DEBUG=False in production!
# DEBUG=True exposes sensitive information and should NEVER be used in production
# For local development, you may set DEBUG=True temporarily
DEBUG=False

# ALLOWED_HOSTS: Comma-separated list of host/domain names this site can serve
# For production, add your actual domain(s): example.com,www.example.com
# For local development, localhost and 127.0.0.1 are sufficient
ALLOWED_HOSTS=localhost,127.0.0.1

# OpenAI API Key (optional - for manual search and maintenance extraction)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here
